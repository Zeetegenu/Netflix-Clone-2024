import React, { useEffect, useState } from 'react'
import axios from "../../utils/axios";
import requests from '../../utils/requests';

const Banner =()=> {
    cnst [DeviceMotionEvent, setMovie] = useState({});
    useEffect(() => {}

)
  return (
    <div>
      
    </div>
  )
}

export default Banner
